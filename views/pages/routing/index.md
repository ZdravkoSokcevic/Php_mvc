#ROUTING

### All your routes are stored into `routing/index.php`

<code>
			$var = 'proba';
			function proba() {
				echo "Syntax highlightning $var";		
			}
			echo $var;
</code>

<code>
let var = "nesto";
</code>